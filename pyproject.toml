[project]
name = "workshop-demos"
version = "0.1.0"
description = "Hands-on demos for the agentic AI workshop."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckduckgo-search>=8.0.0",
    "ipykernel>=6.29.0",
    "jupyter>=1.1.0",
    "langchain>=0.3.0",
    "langchain-community>=0.3.0",
    "langchain-openai>=0.3.0",
    "langgraph>=0.2.0",
    "openai>=1.0.0",
]

[tool.uv]
package = false
